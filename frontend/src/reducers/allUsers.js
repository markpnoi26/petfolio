export default function allUsers(state = [], action) {
  switch(action.type) {
    // case 'ADD_USER':
    //   return [...state, action.payload]
    // case 'REMOVE_PET':
    //   return false
    default:
      return state
  }
}
